# RailMadad Environment Configuration
# Copy this file to .env.local and fill in your API keys

# =============================================================================
# AI Provider Configuration
# =============================================================================
# You can configure one or both AI providers. The app will automatically
# use the available provider, or you can set a preference in the app settings.

# Google Gemini API Key (Primary Option)
# Get your API key from: https://aistudio.google.com/app/apikey
# This uses the native Gemini SDK for optimal performance with Google's models.
VITE_GEMINI_API_KEY=

# OpenRouter API Key (Alternative Option)
# Get your API key from: https://openrouter.ai/keys
# This provides access to 300+ AI models including Gemini, GPT-4, Claude, etc.
VITE_OPENROUTER_API_KEY=

# =============================================================================
# Provider Selection Priority
# =============================================================================
# If both API keys are set, the app will use Gemini by default.
# Users can switch providers in the app settings, which is saved to localStorage.
# 
# Provider comparison:
# - Gemini: Direct access to Google's models, slightly lower latency
# - OpenRouter: Access to 300+ models, unified billing, model flexibility

# =============================================================================
# Development Configuration (Optional)
# =============================================================================

# Cloudflare Tunnel Host (for remote development)
# VITE_TUNNEL_HOST=your-tunnel.trycloudflare.com

# =============================================================================
# Legacy Keys (Backward Compatibility)
# =============================================================================
# These are supported for backward compatibility but VITE_ prefixed versions
# are recommended.

# GEMINI_API_KEY=
# OPENROUTER_API_KEY=
